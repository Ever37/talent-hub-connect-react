import { Card, Container } from '@mui/material';

const NotFound = () => (
  <Container maxWidth="sm">
    <Card style={{ margin: '30px auto', padding: '40px', textAlign: 'center' }}>
      Not found
    </Card>
  </Container>
);

export default NotFound;
